
public class TimeKeeper extends Thread {
  // flag to notify that TimeKeeper thread should keep working
  static boolean keepWorking = true;
  
  // separate thread to process time correction requests
  public void run(){
    
  }
}
